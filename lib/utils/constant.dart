import 'package:uuid/uuid.dart';

const kAppName = 'Great Places';

const kAppBarTitlePlace = 'Your Place';

const kPlaceListIsEmpty = 'Not Places added yet !';

const kAppBarTitleAddPlace = 'Add new Place';

const kInputTileAddPlace = 'Title';

const kButtonlabelAddPlace = 'Add Place';

const uuid = Uuid(); // generate id
